/**
 * Configuração aplicada aos modais do MDB abertos pela aplicação.
 *
 * Espalhe o objeto quando o modal também recebe dados.
 *
 * @example
 * ```ts
 * modalService.open(ServicoFormComponent, { ...ModalConfig, data: { servico } });
 * ```
 */
export const ModalConfig = { modalClass: "modal-dialog-centered" };
